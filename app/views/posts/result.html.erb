<h1>posts#result</h1>
<%= render partial: "layouts/form", locals: { post: @post } %>
<% if @post.valid? %>
  <%#= @post.card_info %>
  <%= @hand %>

  <%#= hand = Poker.new %>
      <%#= hand = hand.poker_flash_straight(card_info) %>
      <%# if @hand == nil %>
        <%# hand = Poker.new %>
        <%#= hand = hand.poker_numerical(card_info) %>
      <%# end %>
<% end %>

<div>
  <% if @post.errors.any? %>
    <div class = "alert">
      <ul>
        <% @post.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
</div>
